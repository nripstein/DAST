# Compute coefficient metadata (number of timepoints, covariates, and their names)

Compute coefficient metadata (number of timepoints, covariates, and
their names)

## Usage

``` r
compute_coef_meta(data)
```

## Arguments

- data:

  A disag_data_mmap object.
