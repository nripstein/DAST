% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitting_mcmc.R
\name{disag_model_mmap_mcmc}
\alias{disag_model_mmap_mcmc}
\title{Fit a multi-map disaggregation model via tmbstan MCMC}
\usage{
disag_model_mmap_mcmc(
  data,
  priors = NULL,
  family = "poisson",
  link = "log",
  time_varying_betas = FALSE,
  fixed_effect_betas = TRUE,
  chains = 4L,
  iter = 2000L,
  warmup = NULL,
  thin = 1L,
  cores = NULL,
  seed = NULL,
  refresh = NULL,
  laplace = FALSE,
  lower = numeric(0),
  upper = numeric(0),
  control = NULL,
  field = TRUE,
  iid = TRUE,
  silent = TRUE,
  starting_values = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{data}{A 'disag_data_mmap' object (from 'prepare_data_mmap()').}

\item{priors}{Optional named list of prior specifications.}

\item{family}{One of 'gaussian', 'binomial', 'poisson', or 'negbinomial'.}

\item{link}{One of 'identity', 'logit', or 'log'.}

\item{time_varying_betas}{Logical; if TRUE, each time point has its own fixed-effect.}

\item{fixed_effect_betas}{Logical; if TRUE (default), active beta coefficients
are sampled as fixed effects. If FALSE, active beta coefficients are included
in the TMB random-effect block.}

\item{chains}{Integer >= 1; number of MCMC chains.}

\item{iter}{Integer >= 1; total Stan iterations per chain, including warmup.}

\item{warmup}{Integer >= 0 and less than \code{iter}; warmup iterations per
chain. Defaults to \code{floor(iter / 2)}.}

\item{thin}{Integer >= 1; thinning interval.}

\item{cores}{Integer >= 1; number of cores passed to Stan. Defaults to
\code{getOption("mc.cores", chains)}.}

\item{seed}{Optional positive integer seed.}

\item{refresh}{Optional integer >= 0; Stan progress refresh interval.}

\item{laplace}{Logical; passed to \code{tmbstan::tmbstan()}. Defaults to
\code{FALSE} for full posterior sampling.}

\item{lower}{Numeric lower bounds passed to \code{tmbstan::tmbstan()}.}

\item{upper}{Numeric upper bounds passed to \code{tmbstan::tmbstan()}.}

\item{control}{Optional list passed to \code{rstan::sampling()}.}

\item{field}{Logical: include the spatial random field?}

\item{iid}{Logical: include polygon-specific IID effects?}

\item{silent}{Logical: if TRUE, suppress TMB/tmbstan console output.}

\item{starting_values}{Optional named list of starting parameter values.}

\item{verbose}{Logical: if TRUE, print total runtime.}

\item{...}{Additional arguments passed through to \code{tmbstan::tmbstan()}
and \code{rstan::sampling()}.}
}
\value{
An object of class 'disag_model_mmap_mcmc' with components
  \code{stanfit}, \code{obj}, \code{data}, and \code{model_setup}.
}
\description{
Builds the shared TMB ADFun object for a multi-map disaggregation model, then
samples from it with \code{tmbstan::tmbstan()}. This engine supports
parameter estimation only; prediction is not implemented for MCMC fits.
}
